{
  "name": "vibecheck",
  "private": true,
  "version": "0.1.0",
  "description": "VibeCheck — the agentic CMS you talk to. Schema-safe, git-backed, headless. You bring the vibe; it does the check.",
  "type": "module",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build:schema": "npm run build -w @vibecheck/schema",
    "build:site": "npm run build -w site",
    "typecheck:mcp": "npm run typecheck -w @vibecheck/mcp",
    "build": "npm run build:schema && npm run build:site && npm run typecheck:mcp"
  }
}
